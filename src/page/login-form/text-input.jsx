import React from 'react';

/** @param {Object} param0
 * @param {React.ReactNode} param0.children
 */
export const TextInput = ({children}) => {
  return <label className='login__label'>{children}</label>;
};
